# --- GUI -------------------------------------------------------------
PyQt6>=6.6,<7.0
PyQt6-Qt6>=6.6,<7.0

# --- Data / persistence ---------------------------------------------
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0

# --- Validation / config --------------------------------------------
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0

# --- Hardware interfaces --------------------------------------------
pyserial>=3.5,<4.0

# --- Future (Stage 2: LAN server integration) -----------------------
httpx>=0.27,<1.0

# --- Security -------------------------------------------------------
# Lesson 11 — proper password hashing for USER_MASTER.
argon2-cffi>=23.1,<25.0

# --- Reporting ------------------------------------------------------
openpyxl>=3.1,<4.0

# --- Testing --------------------------------------------------------
pytest>=8.0,<9.0
pytest-qt>=4.4,<5.0

# --- Packaging ------------------------------------------------------
pyinstaller>=6.3,<7.0
